import React, { PropTypes } from 'react';

class Timer extends React.Component {
    render () {
        return (
            <div>
                Timer.jsx
            </div>
        );
    }
}

export default Timer;
